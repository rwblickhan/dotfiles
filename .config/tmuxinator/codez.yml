# /Users/rwblickhan/.config/tmuxinator/codez.yml

name: codez
root: ~/sandbox/asana

windows:
  - codez:
      layout: even-horizontal
      panes:
        - fish -c 'git up'
        - fish -c 'z sand sync -k webapp'
