# /Users/rwblickhan/.config/tmuxinator/codez.yml

name: codez
root: ~/sandbox/asana

pre_window: fish
windows:
  - codez:
      layout: main-vertical
      panes:
        - git up
        - z sand sync -k webapp
        - z test --diff
